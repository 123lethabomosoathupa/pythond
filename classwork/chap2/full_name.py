# Store the first name in a variable
first_name = "franklin"

# Store the last name in a variable
last_name = "saint"

# Combine the first and last names into a full name using an f-string
full_name = f"{first_name} {last_name}"

# Print the full name to the console
#print(full_name)
#message = f"Hello, {full_name.title()}!"
#print(message)
print(f"Hello, {full_name.title()}!")